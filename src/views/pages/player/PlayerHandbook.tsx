import { StateProps } from '@engine/types'

function PlayerHandbook({ state, dispatch }: StateProps) {
    return (
        <div>
            <p style={{ color: 'rgba(255,255,255,0.5)' }}>Coming soon...</p>
        </div>
    )
}

export default PlayerHandbook
